import { Router } from "express";

export const authRouter = Router();

// Auth endpoints (register, login, refresh, logout) are implemented
// in the next stage once role/registration requirements are confirmed.
